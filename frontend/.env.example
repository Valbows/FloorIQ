# Frontend environment configuration template
# Prefix client-side variables with VITE_ so Vite exposes them at build time.

VITE_API_URL=https://flooriq-backend.onrender.com
VITE_GOOGLE_MAPS_API_KEY=

# Optional: point to staging backend or enable feature flags as needed.
VITE_FEATURE_FLAGS=
